---
import type { ImageFunction } from 'astro:content';

interface Props {
  numeroEmpresas: number;
  tipoTarjeta: string;
  iconTarjeta: 
}

const { numeroEmpresas, tipoTarjeta } = Astro.props;
---

<div class="bg-greyBlack flex flex-row rounded-md p-3 justify-between w-46">
  <Image src={myImportedImage} alt="descriptive text" />
  <div class="flex flex-col text-white">
    <h1 class="text-4xl font-medium">{numeroEmpresas}</h1>
    <h2>{tipoTarjeta}</h2>
  </div>
</div>
