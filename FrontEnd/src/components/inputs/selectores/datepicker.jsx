import { DatePicker } from '@tremor/react';

/**
 * Componente que renderiza un selector de fecha.
 *
 * @returns {JSX.Element} - Un elemento que representa un selector de fecha.
 */
export function DatePickerP() {
  return <DatePicker className="mx-auto max-w-sm" />;
}
